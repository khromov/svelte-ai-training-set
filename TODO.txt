x Use BATCH processing
* Some whitespace issues in svelte.md like <buttonon:click{onclick}>
* Add Svelte 5 Preprompt for correct examples 
* Remove legacy blocks
* OR ask the model to only directly quote the documentation.
* Ask the model to ONLY quote direct code examples from the docs.
* "Wash" the data to remove incorrect examples
* Allow temperature control
* Save to dated file + temperature in filename
* Merge utility npm run merge from output/*.jsonl to output/merged.json